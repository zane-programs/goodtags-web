# Vite content-hashes everything under /assets, so it never needs revalidating. All
# other files keep Cloudflare's default (revalidate with an ETag), which is what the
# service worker, app shell, manifest and catalog need to pick up new versions.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
